#compdef rbfu rbfu-env
local ruby version
local -a versions real
for ruby in ~/.rbfu/rubies/*(N-/) ; do
  version=@$ruby:t
  real=( $ruby(:A:t) )
  versions+=( $version:$real )
done
_arguments \
  "1:version:((\$versions))" \
  '(-):command: _command_names -e' \
  '*::args: _normal'
